## Changes Made

### 1. Updated icon color in ExportPage Config Summary for coral preset
- **File:** `src/pages/ExportPage.tsx`
- **Change:** Removed `text-white` Tailwind class from the `coral` preset icon circle and added an inline `style` prop with `color: 'rgb(240, 10, 10)'`
- **Before:** `${preset.color === 'coral' ? 'bg-clay-coral text-white' : ''}`
- **After:** `${preset.color === 'coral' ? 'bg-clay-coral' : ''}` + `style={preset.color === 'coral' ? { color: 'rgb(240, 10, 10)' } : undefined}`
- **Revert:** Restore `text-white` to the coral class string, remove the `style` prop

## Revert Status
- [ ] Change 1 - Coral preset icon color
